/**
 * Optional: set window.__AUTH_SERVICE_URL__ for admin login.
 * Overwritten at Docker build with AUTH_SERVICE_URL from .env.
 * For local dev without Docker, config.js uses fallback (e.g. auth.{domain}).
 */
// window.__AUTH_SERVICE_URL__ = undefined;
